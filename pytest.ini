[pytest]
# This section tells pytest to load environment variables from the specified .env file.
# Useful for setting PYTHONPATH and secrets like API keys during test runs.

env_files =
    .env  # Loads environment variables like PYTHONPATH and SUPABASE credentials from .env
