# personal_knowledge_engine/cli/notes_cli.py

"""
CLI entrypoints for listing, previewing, and upserting notes.

This module will wire into the DummyClient for tests and the real Supabase client in production.
"""


def main():
    # Temporary placeholder so the command runs without errors.
    print("Notes CLI scaffold loaded.")
