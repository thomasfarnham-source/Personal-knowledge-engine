# supabase.py

from supabase import create_client
from config import SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY

# Initialize the Supabase client using the project URL and service role key
supabase = create_client(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)

# Now you can import 'supabase' in other files (like main.py) to interact with your database